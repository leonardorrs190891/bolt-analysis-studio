# Pré-registro B1-v3 — F4 L1 mecanismo v2, candidato (c) flank_s_crit

**Data:** 2026-07-22 · **Branch:** feature/l1-v2 (worktree C:\basl1v2) · **Executor:** sessão
mestre (prompt-mestre §4). Escrito ANTES de qualquer fit — imutável.

## Candidato

`flank_s_crit` [m]: limiar de slip do flanco — wear ∝ (s_th − s_crit)+^exp; s_crit=0 =
bit-idêntico ao v1 (TDD: tests/test_l1v2_flank_scrit.py, 26 passed). Racional: a falsificação
T4 provou que power-law puro não é fraco no NÍVEL e forte no SLOPE ao mesmo tempo (k saturava
no limite inferior; exp livre já falhou na tentativa 1 do T4). O limiar zera o canal em
amplitude baixa e maximiza d(wear)/dA_F perto do limiar. Precedente físico: regime
stick/shakedown do fretting (Mäntylä 2020; Juoksukangas 2016). `flank_amp_exp=1,5` HERDADO
(T4 tentativa 2, Liu 2020); só {k_wear_flank, flank_s_crit} fitam (2 DOF per-rig).

## Gate (imutável — plano-mestre §4.2)

- **G4-a (slope):** fit Rig A (Liu2017: 4 curvas do sweep A_F 7,5–12,5 kN, curva completa com
  FIT_CAP=1e4 na fase de busca — justificativa idêntica ao T4) → passada FULL-RES (1e6) com os
  parâmetros fitados → slope linear d(F_fim/F0)/dA_F ∈ **[−4,4e-5, −1,1e-5]/N**.
- **G4-b (R5):** com o v2 fitado per-rig, os casos R5 de **liu2020 (9, axial coatings)**
  atingem tripé<0,1 usando µ por coating do KB (zinc 0,150/DLC 0,126, mu_thread_anchor) +
  flank v2 per-rig. **zhang18/19 (13, TRANSVERSAIS)**: o canal L1 é axial-força por predicado;
  se após o v2 axial os casos transversais permanecerem >0,1, isso NÃO reprova o candidato
  (documenta-se: o gap deles exige a extensão transversal do flanco — decisão de forma
  separada, fila) — MAS o G4-b mínimo exige ≥6/9 do liu2020 no tripé.
- **G4-c (zero regressão):** re-sim de 8 controles (liu2017 F0-sweep ×2, li2022ti ×2 com o
  per-rig adotado F2-P2.1, liu2016 ×1, transversais ×3) bit-idênticos onde o cfg não mudou.
- **Sem regressão no F0-sweep:** os 5 casos liu2017_axial_F0_* (hoje ≤0,0205) não pioram >0,005.
- Máx. **2 preregs** para este candidato; FAIL2 → candidato (b) debris (novo prereg) e, se
  também FAIL2, rodada 6 + **PARE** (professor) — caminho do plano §4.3.

## Fit (fase de busca)

Grade: log10(k_wear_flank) ∈ {−14 … −11,5} (6) × s_crit/s_ref ∈ {0; 0,4; 0,6; 0,75; 0,9} (5)
com s_ref = s_th(A_F=10 kN) do rig Liu2017; nas 4 curvas do sweep A_F @ cap 1e4 + as 5 do
sweep F0 @ cap 1e4 como anti-regressão na própria busca. Escore: (gate-distância do slope
estimado no cap, mediana MAE). Full-res só na célula vencedora (+ vizinhança 3×3 se
borderline). Sims estimadas: 30 células × 9 curvas @1e4 (~segundos) + ≤9×4 full-res.

## Registro

Resultado em New_Theory/f4_b1v3_result.json (worktree); adoção NÃO acontece no branch —
se PASS, segue o protocolo F0 (merge gateado + adoção per-rig no main, F4.4).
