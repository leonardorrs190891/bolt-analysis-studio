# Pré-registro — rota E2: adoção LIU_2025 com `N_f` INPUT-DE-PAPER por curva

**Data:** 2026-07-28 · **Autorização:** professor, *"pré-registre a rota E2"*
**Antecessor:** adoção com relógio PREDITIVO, **revertida pelo gate cego A1** (`amp0p8`
+0,111 — §4.52). A causa foi o relógio (±36 % contra ≤5 % de orçamento), não a forma.
**Status:** CONGELADO — **execução NÃO autorizada ainda** (aguarda a palavra do professor).
**Contas pré-congelamento:** `New_Theory/liu2025_e2_contas.{py,log,json}` — RODADAS.

---

## 0. O que muda em relação à adoção revertida — UMA coisa

O relógio deixa de ser previsto e passa a ser **lido**: `N_f` por curva vem da coluna
*cycles to end* da matriz de ensaios — **a mesma coluna de onde já vêm os `trim_n_max`**
(input-de-paper, 7 números). Precedente adotado: `LI_2022_TRIBOINT` roda com `fat_C1`
ancorado no `N_frat` medido. **Custo honesto declarado:** a claim cai de *"o modelo prevê
a vida"* para ***"o modelo prevê a curva dada a vida"*** — e com relógio lido a rampa já
provou 10/10 no núcleo (gates v2).

**Cegueira:** medido pré-congelamento = os 7 `fat_C1` (usando SÓ os alvos `N_f`), a
tabela analítica α(D_trim), e a métrica pós-trim da **curva de projeto `amp0p8`** (a que
matou a adoção anterior — o teste mais duro). **Cego para os gates:** a métrica pós-trim
das outras 6 curvas (trajetos descartados sem avaliar) e as 195 de fora.

## 1. Receita congelada (nada é fitado na execução)

**Bloco de grupo** (chaves `LIU_2025`, `LIU_2025_amp0p4`, `LIU_2025_amp0p5`), igual ao
da adoção anterior **menos o C1 global**:

```json
{"fatigue_enabled": true, "fat_stress_mode": "bending", "fat_Kt": 0.588,
 "fat_sigma_uts": 800e6, "fat_sigma_knee": 0.0, "fat_sigma_endurance": 1.0,
 "fat_m1": 3.12, "fat_ramp_D_on": 0.75, "fat_ramp_q": 8.0}
```

**`fat_C1` POR CURVA** — via `per_case` na chave `LIU_2025` (tokens sem colisão de
substring, conferidos) e **escalar** nas duas chaves de subgrupo (que vencem por prefixo):

| curva | `fat_C1` (FIXADO) | resíduo do relógio |
|---|---:|---:|
| amp0p25 | 3,19733e32 | 0,9965 ✓verificado |
| amp0p3 | 4,32843e32 | 1,0026 ✓ |
| amp0p4 *(subgrupo)* | 3,27225e32 | ~1,006 (pós-correção, não re-verificado) |
| amp0p5 *(subgrupo)* | 3,26716e32 | ~1,009 (idem) |
| amp0p6 | 3,26567e32 | **não re-verificado** (↓ nota) |
| amp0p8 | 5,22562e32 | **1,0709 ✓verificado** (curva de projeto) |
| fig2_single | 3,83898e32 | 1,0013 ✓ |

> **Nota de honestidade (ancoragem):** o protocolo de 2 passes assume `N_pred ∝ C1`;
> nas duas curvas onde a janela da rampa mais interage com o Goodman (`amp0p6`,
> `amp0p8`) a linearidade degrada e o valor final fica com resíduo de ~7–11 %, não
> ~1 %. **Direção conservadora:** relógio ATRASADO empurra `D_trim` para BAIXO de
> `D_on` (na `amp0p8` verificada: D_trim = 11500/15421 = 0,746 < 0,75 ⇒ α = 0 exato
> na janela). O risco residual seria relógio ~10 % ADIANTADO na `amp0p6` (cega):
> mesmo aí, α(D_trim) ≈ 7e-5 — quatro ordens abaixo de qualquer efeito na métrica.

**Procedência:** `N_f` por curva = **input-de-paper** (mesma coluna dos `trim_n_max` —
zero DOF de forma novo; os 7 `C1` são tradução mecânica dos 7 `N_f`); demais constantes
com as classes já declaradas no prereg anterior (`m1` medido-de-dados; `Kt` per-rig via
Table 2; `uts` handbook 8.8; `D_on` handbook; `q` per-rig reuso).

## 2. As contas (satisfazibilidade medida)

**Previsão central, verificada:** com relógio lido, `α` na borda do trim:

| curva | amp0p25 | amp0p3 | amp0p4 | amp0p5 | amp0p6 | amp0p8 | fig2 |
|---|---:|---:|---:|---:|---:|---:|---:|
| D_trim | 0,727 | 0,720 | 0,779 | 0,790 | 0,744 | 0,799 | 0,800 |
| α(D_trim) | **0** | **0** | 3,5e-8 | 3,9e-7 | **0** | 2,0e-6 | 2,6e-6 |

⇒ **a rampa é numericamente nula em toda janela de métrica** — o oposto estrutural da
adoção revertida, onde o relógio adiantado da `amp0p8` punha o colapso INTEIRO na janela.

**Curva de projeto (`amp0p8`, a assassina da adoção anterior), medida:**
store 0,0487/0,0853 → com E2 **0,0487/0,0853** (Δ **−0,00009 / +0,00000**) — **PASSA**.

## 3. GATES (imutáveis; execução aguarda autorização)

**E1 — tripé mantido e nenhuma curva pior (CEGO).** As 7 curvas, métrica canônica
pós-trim: **7/7 no tripé** E nenhuma piora **> +0,01 em MAE** (PR-37′). Cegas: 6 de 7.

**E2g — as 195 de fora não se movem.** Métricas idênticas (ignorando `generated_at` e
`engine_fingerprint`).

**E3 — fingerprint novo e ÚNICO nos 203, com o conserto do bug de cobertura.** A
execução re-carimba `exemplo_m12_sintetico` à parte
(`parallel_batch --cases exemplo_m12_sintetico --store` após o batch cheio) — fecha o
achado colateral do A3 (§4.52) em vez de tropeçar nele de novo.

**E4 — informacional (sem limiar):** res.máx full-curve por curva (expectativa: a rampa
no lugar certo, como nos gates v2 com relógio lido — núcleo 10/10); decomposição com o
canal `fatigue`; posição do N_D; report HTML regenerado.

### Interpretação pré-declarada

| resultado | ação |
|---|---|
| E1–E3 ✓ | **adoção definitiva** — o estágio 3 entra no canônico por física dada a vida; re-baselinar CLAUDE.md (item 12 + fingerprint do Estado da meta), §4.53 no doc vivo, verdict do config |
| **E1 ✗** | **reverter pelos backups** (config + store); capacidade fica; registrar causa por curva |
| E2g ✗ ou E3 ✗ | bug de execução; consertar sem reinterpretar |

## 4. O que NÃO está sendo proposto

- **Não** prever a vida — `N_f` é **input** (7 números da mesma coluna dos trims).
- **Não** mexer em trims, métrica, ou qualquer outra fonte.
- **Não** re-fitar nada na execução (os 7 `C1` estão fixados acima).
- **Não** re-verificar/retunar os 2 resíduos de ~7–11 % pós-congelamento — seria mover
  a trave; eles estão declarados e a direção é conservadora (§1, nota).

## 5. Reprodutibilidade

```bash
py -3.12 New_Theory/liu2025_e2_contas.py                 # contas (ja rodadas)
py -3.12 New_Theory/parallel_batch.py --workers 6 --store
py -3.12 New_Theory/parallel_batch.py --cases exemplo_m12_sintetico --store
py -3.12 New_Theory/liu2025_adocao_gates.py <backup>     # E1/E2g/E3 (mesmo arnes)
python -m bolt_analysis_studio.validation.report
```
