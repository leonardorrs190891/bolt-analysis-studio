# Prereg P3-R3 — LU_2024, rodada final da re-leitura (baseline P5+P2)

**2026-07-31 (noite)** · autorização em sessão: *"autorizado P5 e P2 como
recomendado, siga para R3"*. Baseline: P0 (fig20=1,0 mm) + P5 (`T4Nm`
DECLARADA por escopo, palavras do paper) + P2 (`amp1p0` = duplicata da
`T22Nm`, fora do censo; denominador 201). Gates imutáveis; uma execução.

## Consertos estruturais vs R2 (ambos medidos lá)

1. **O c1 vira RESTRIÇÃO do estágio B** (não só gate final): sobrevivem do
   estágio A apenas pontos com `MAE(c1) ≤ 0,08` — mata a deriva do
   otimizador que reprovou o G2 da R2 por 0,0904 (o J das leituras escolhia
   sobrevivente de c1 pior).
2. **G4 sem os dois bloqueadores decididos**: `T4Nm` está DECLARADA (P5) —
   proteger as métricas dela preservaria o bloqueador perpétuo que as duas
   rodadas mediram; ela é simulada e REPORTADA informacionalmente.
   `amp1p0` é duplicata (P2) — espelho da T22, fora de tudo, reportada.
   **G4 = 8 curvas** (4 leituras + 4 held).

## Congelado

* `c_bend = 30,0` (âncora Fig. 21; G1 confere a banda [90,107]e6).
* Split: LEITURAS {amp0p25, T22Nm, T10Nm, amp2p0} · HELD {amp0p5, amp1p5,
  T16Nm, T28Nm} (idêntico à R2 menos a duplicata).
* Estágio A: grade R2 `{emb 8/11/14 µm × frac 0,3/0,4/0,5 × q 0,5/1/2 ×
  N_emb 0,5/1}`, sobreviventes = monotonia ∧ c1(amp0p25)≤0,22 ∧
  **MAE(c1)≤0,08**; ≤4 por MAE(c1).
* Estágio B: `{k_ratchet 0,005/0,01/0,02 × floor 0/0,10/0,21}` nas 4
  leituras; escolha = máx. leituras com MAE≤0,05, desempate por Σσ².
* Gates: G1 banda k_tr · G2 MAE(c1)≤0,08 ∧ monotonia (por construção; fica
  como cheque) · G3 `veredicto_generalizacao` (σ E MAE) nas 4 held ·
  G4 nenhuma das 8 piora >+0,01 em nenhuma perna vs pós-P0 ·
  G5 mediana da fonte (9 do censo) −30 % OU ≥2 leituras no tripé efetivo.

**PASSA ⇒ adotar por delegação** (entry LU_2024: c_bend, emb_depth,
emb_load_frac, emb_slip_gate, N_emb, k_ratchet, loose_arrest_floor;
procedência: âncoras Fig. 21 + Tabelas 8/9 + preregs R1/R2/R3) + pipeline
completo. NAO PASSA ⇒ registrar e devolver: o que sobra é P4 (Fig. 14) e
P6 (forma plástica, PR-3).
