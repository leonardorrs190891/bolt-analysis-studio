# Prereg P3 — re-leitura do LU_2024 sob o drive CORRETO, com as âncoras do paper

**2026-07-31 (noite)** · plano `lu2024_plano_melhoria.md` (P0/P1 feitos:
fig20→1,0 mm; retratações) · autorização do professor em sessão: *"vai para
o P3, decido P2/P5 depois"*. Store baseline pós-P0 (fig20 re-simuladas —
T4 mae 0,317, T10 0,209, T22 0,110; fig18 quase inalteradas). Gates
imutáveis.

## 0. Diagnóstico que fundamenta (tudo medido hoje)

* **k_tr (Fig. 21)**: modelo 39,3e6 N/m constante vs medido 50,4→115,2e6
  CRESCENDO com F₀ (exp≈0,42). No F₀ central (22 N·m): modelo 2,5× mole ⇒
  **c_bend lido da âncora: 12 → ≈4,8** (k_tr∝1/c_bend na região; a
  dependência de F₀ fica como forma ausente documentada, fora deste prereg).
* **1º ciclo (Tabelas 8/9)**: grade 64 pontos × 9 condições ⇒ (i) `N_emb=3`
  espalha o alvo — o dado põe o grosso no c1 ⇒ eixo N_emb∈{0,5, 1, 2};
  (ii) `frac` na borda 0,55 ⇒ estender a 0,7; (iii) q (emb_slip_gate) ≈ 0;
  (iv) **T4 poluído** (estágio-2 dispara no c1: modelo 0,77 vs dado 0,16) ⇒
  T4 FORA da âncora-c1 e FORA das leituras (pendente P5).
* **Resíduo pós-P0**: sobre-colapso NO MEIO em quase todas (μ_meio −0,06..
  −0,35) ⇒ k_ratchet para baixo e/ou floor re-lido; tombo de c1 ausente nas
  amplitudes altas (μ_início +0,10..+0,15).

## 1. Split mecânico (identidade do dado; congelado)

* **LEITURAS (5)**: `amp0p25` (âncora estática/delta_free), `amp1p0` +
  `T22Nm` (MESMO teste — os dois no mesmo lado, vazamento zero), `T10Nm`,
  `amp2p0` (extremo de amplitude).
* **HELD (4)**: `amp0p5`, `amp1p5`, `T16Nm`, `T28Nm` — 2 por varredura.
* `T4Nm`: fora de leituras E held (pendente P5; entra só no gate de acervo).

## 2. Procedimento congelado (2 estágios, sem re-otimizar depois)

* **Estágio A (triagem-c1, barata)**: grade `{emb_depth 0/6/11 µm ×
  emb_load_frac 0,5/0,6/0,7 × N_emb 0,5/1/2}` com `c_bend=4,8` fixo
  (âncora), q=0, demais campos da config atual. Métrica: MAE dos c1 vs
  Tabelas 8/9 **nas 8 condições sem T4**. Sobrevivem os ≤4 melhores.
* **Estágio B (full-curve nas LEITURAS)**: sobreviventes × `{k_ratchet
  0,005/0,01/0,02/0,04 × loose_arrest_floor 0/0,10/0,21}`. Escolha: máximo
  de leituras com MAE ≤ 0,05, desempate por Σσ². As HELD não são olhadas.

## 3. Gates (imutáveis)

* **G1 âncora-k_tr**: com c_bend=4,8, k_tr do modelo ∈ [90, 107]e6 N/m
  (±10 % do medido no F₀ central).
* **G2 c1**: no ponto escolhido, MAE(c1) das 8 condições ≤ 0,08 (metade do
  0,155 atual) E a ordenação por amplitude do c1 correta (monotônica
  0,25→2,0 mm).
* **G3 generalização**: `veredicto_generalizacao` (σ e MAE) nas 4 HELD,
  tol +0,01 ⇒ `generaliza=True` nas duas métricas.
* **G4 acervo**: nenhuma das 10 curvas piora >+0,01 em nenhuma perna vs o
  baseline PÓS-P0 (o custo do input já foi pago; daqui só se melhora).
* **G5 rendimento**: mediana do MAE da fonte cai ≥30 % vs pós-P0 (hoje
  ~0,21 nas fig20) OU ≥2 leituras entram no tripé efetivo.

**PASSA = G1∧G2∧G3∧G4∧G5 ⇒ adotar por delegação** (campos re-lidos na
entry LU_2024 com procedência "âncoras Fig. 21/T8/T9 do paper, prereg P3");
re-carimbo + reports + docs no mesmo push. Reprovação em G3 ⇒ FALSIFICADO
como leitura per-par (registrar); em G2 com G3 ok ⇒ PARCIAL (não adota;
o c1 exige a forma plástica do P6).

## 4. O que este prereg NÃO faz

Não toca as 7 declaradas (colapso segue metric-limited); não decide P2/P5
(dedup e T4 — professor); não constrói forma nova (a dependência k_tr(F₀)
e a plasticidade de furo ficam documentadas para o P6/PR-3).
