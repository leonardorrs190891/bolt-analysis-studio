# Prereg P3-R2 — LU_2024, rodada 2 da re-leitura (canto certo da grade)

**2026-07-31 (noite)** · R1 = `2026-07-31-lu2024-p3-prereg.md`, executada:
**NAO PASSA (G1+G4), com G2/G3/G5 PASSANDO** — held-out generalizou nas duas
métricas (σ mediana 0,106→0,056; MAE 0,119→0,078), c1 0,155→0,074 com
monotonia, mediana da fonte −32 %. As duas falhas têm causa medida:

* **G1**: assumi k_tr∝1/c_bend SEM sonda de direção (violação da regra do
  próprio repo) — a relação é DIRETA. Sonda de 6 pontos:
  **c_bend=30,0 → k_tr=98,3e6 N/m** contra 98,4e6 medido na Fig. 21 —
  a âncora fecha ao décimo; c_bend deixa de ser fitado.
* **G4**: o vencedor do estágio B (emb=0, frac=0,5, **q=0**) derruba 50 %
  fracional UNGATED na `amp0p25` sub-limiar (dado: 17–22 %) — mae
  0,144→0,240. A física do paper pede **estático (emb_depth≈VDI 11 µm ⇒
  ~15 % a F₀=12 kN) + fracional GATEADO por slip (q>0)** — canto que o
  estágio A da R1 não cobriu (frac começava em 0,5 e q parava a decisão no
  c1 agregado, sem restrição na amp0p25).

## Leitura congelada da R2

* `c_bend = 30,0` (ANCORADO, Fig. 21 — fora da otimização).
* Estágio A′ (triagem-c1, 8 condições sem T4): grade
  `{emb_depth 8/11/14 µm × emb_load_frac 0,30/0,40/0,50 ×
  emb_slip_gate 0,5/1,0/2,0 × N_emb 0,5/1,0}` com **restrição
  c1(amp0p25) ≤ 0,22**; sobrevivem ≤4 por MAE(c1) com monotonia.
* Estágio B (full-curve nas 5 LEITURAS, split IDÊNTICO ao R1 — amp1p0≡T22
  juntas): `{k_ratchet 0,005/0,01/0,02 × floor 0/0,10/0,21}`.
* Gates **idênticos** aos da R1 (G1 banda [90,107]e6 · G2 ≤0,08+monotonia ·
  G3 held 4 curvas sd E mae · G4 acervo 10 vs pós-P0 · G5 mediana −30 % OU
  ≥2 leituras no tripé). Held intocadas até o G3.

PASSA ⇒ adota por delegação (procedência R1+R2 nas entradas; autorização em
sessão: "vai para o P3"). NAO PASSA de novo ⇒ registra e devolve ao
professor com o mapa (o resto é P6/forma).
