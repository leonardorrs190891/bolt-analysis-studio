# Report de Validação v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Steps em checkbox `- [ ]`.

**Goal:** Report por caso reformulado (spec `2026-07-10-report-v2-design.md`): resíduo assinado do erro artigo-vs-modelo, seção do modelo MSD gerada do modelo REAL com tabela de elementos + carregamento + glossário PT (reproduzível só lendo), formatação numerada 1-6 + print-friendly.

**Architecture:** Tudo em `validation/report_html.py`: `_svg_residual` (SVG do resíduo com banda ±MAE), `_stage_maes` (janelas 0-10/10-70/70-100% dos ciclos), `_error_narrative` (texto interpretado), `_msd_section(rec)` (lazy `gui_bridge.build_case_model`; diagrama de cadeia SVG + tabelas de elementos/carregamento + glossário `_GLOSSARY` + passo-a-passo; fallback textual p/ família `other`/erro de import). Reestrutura do `case_report_html` em seções 1-6; `@media print` no `_CSS`.

**Tech Stack:** stdlib + numpy; SVG string; sem libs novas.

## Global Constraints

- `utf-8`, `ast.parse`, commit por tarefa, `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; não tocar runner/store/GUI nem foreign files.
- Fatos verificados: `MSDParameters.k/c/m` (`element.py:1115`), `MaterialData.name` (`element.py:807`), `MSDElementData.{id,name,type,material,msd,preload_percent_yield}` (`element.py:1309`); `build_case_model` levanta `ValueError` p/ família `other`; `model.global_loading.{type,control_mode,F_preload,delta_amplitude,frequency,n_cycles,mu_initial}`; `model.elements` é a lista.

---

## Task 1: Resíduo assinado + métricas interpretadas + MAE por estágio

**Files:**
- Modify: `src/bolt_analysis_studio/validation/report_html.py`
- Test: `tests/test_validation_report_html.py` (append)

**Interfaces:**
- Produces: `_svg_residual(dx, dy, mx, my, mae) -> str` (SVG `class="plot residual"`); `_stage_maes(cd, rd, pred) -> dict` (`{"I": float|None, "II": ..., "III": ...}`); `_error_narrative(result, resid) -> str` (parágrafo PT); seção "3. Resultado e erro" no `case_report_html`.

- [ ] **Step 1: Testes falhando** — append:

```python
def test_error_section_has_signed_residual_and_narrative():
    from bolt_analysis_studio.validation.case_registry import all_records
    from bolt_analysis_studio.validation.report_html import case_report_html
    rec = next(r for r in all_records()
               if r.case_class == "full_curve" and r.family == "transverse"
               and r.gallery_entry is not None)
    html = case_report_html(rec, _fake_result(rec.case_id))
    assert "residual" in html                       # painel dedicado
    assert "sobre-prediz" in html or "sub-prediz" in html
    assert "pontos percentuais" in html             # narrativa interpretada
    assert "Estágio I" in html                      # MAE por estagio


def test_stage_maes_windows():
    import numpy as np
    from bolt_analysis_studio.validation.report_html import _stage_maes
    cd = np.linspace(0, 1000, 101)
    rd = np.ones(101)
    pred = np.ones(101); pred[:11] += 0.1           # erro so no estagio I (0-10%)
    st = _stage_maes(cd, rd, pred)
    assert st["I"] > 0.05 and st["II"] < 1e-9 and st["III"] < 1e-9
```

- [ ] **Step 2: Rodar e ver falhar.**
- [ ] **Step 3: Implementar** em `report_html.py`:

```python
def _stage_maes(cd, rd, pred):
    """MAE por estagio (janelas de ciclo 0-10% / 10-70% / 70-100% — proxy das
    fases assentamento/perda/cauda). None quando a janela nao tem pontos."""
    if not len(cd):
        return {"I": None, "II": None, "III": None}
    span = cd[-1] - cd[0] or 1.0
    frac = (cd - cd[0]) / span
    err = np.abs(np.asarray(pred) - np.asarray(rd))
    out = {}
    for name, lo, hi in (("I", 0.0, 0.10), ("II", 0.10, 0.70), ("III", 0.70, 1.001)):
        m = (frac >= lo) & (frac < hi)
        out[name] = float(err[m].mean()) if m.any() else None
    return out


def _svg_residual(dx, dy, mx, my, mae, W=560, H=170):
    """Residuo ASSINADO modelo-artigo vs ciclos + banda sombreada +-MAE +
    linha zero (spec §2.1: direcao do erro visivel)."""
    if not len(dx) or not len(mx) or mae is None:
        return ""
    resid = np.interp(dx, mx, my) - np.asarray(dy)
    ML, MR, MT, MB = 56, 16, 12, 34
    rmax = max(float(np.abs(resid).max()), mae, 1e-6) * 1.15
    xmax = max(float(dx[-1]), 1.0)

    def lx(v):
        return ML + v / xmax * (W - ML - MR)

    def ly(v):
        return MT + (1 - (v + rmax) / (2 * rmax)) * (H - MT - MB)

    s = [f'<svg viewBox="0 0 {W} {H}" class="plot residual" '
         f'xmlns="http://www.w3.org/2000/svg">']
    # banda +-MAE
    s.append(f'<rect x="{ML}" y="{ly(mae):.1f}" width="{W-ML-MR}" '
             f'height="{max(ly(-mae)-ly(mae), 1):.1f}" class="band"/>')
    for v, cls in ((0.0, "zl"), (rmax * 0.87, "gl"), (-rmax * 0.87, "gl")):
        s.append(f'<line class="{cls}" x1="{ML}" y1="{ly(v):.1f}" '
                 f'x2="{W-MR}" y2="{ly(v):.1f}"/>')
    for v in (rmax * 0.87, 0.0, -rmax * 0.87):
        s.append(f'<text class="tk" x="{ML-6}" y="{ly(v)+3:.1f}" '
                 f'text-anchor="end">{v:+.3f}</text>')
    s.append('<polyline class="rl" fill="none" points="'
             + " ".join(f"{lx(x):.1f},{ly(r):.1f}" for x, r in zip(dx, resid))
             + '"/>')
    s.append(f'<text class="axl" x="{(ML+W-MR)/2:.0f}" y="{H-3}" '
             f'text-anchor="middle">ciclos N</text>')
    s.append(f'<text class="tk" x="{W-MR}" y="{MT+8}" text-anchor="end">'
             f'+ modelo sobre-prediz</text>')
    s.append(f'<text class="tk" x="{W-MR}" y="{H-MB-2}" text-anchor="end">'
             f'&#8722; modelo sub-prediz</text>')
    s.append('</svg>')
    return "".join(s)


def _error_narrative(result, dx, dy, mx, my):
    if result.mae is None:
        return ""
    bits = [f"Em média o modelo erra <b>{100*result.mae:.1f} pontos percentuais</b> "
            f"de F/F&#8320; (MAE {result.mae:.4f})"]
    if result.maxerr is not None:
        bits.append(f"pior ciclo: {result.maxerr:.3f} @ N={int(result.maxerr_at or 0)}")
    if len(dx) and len(mx):
        resid = np.interp(dx, mx, my) - np.asarray(dy)
        frac_over = float((resid > 0).mean())
        lean = ("sobre-prediz o afrouxamento na maior parte da curva"
                if frac_over > 0.6 else
                "sub-prediz o afrouxamento na maior parte da curva"
                if frac_over < 0.4 else "erro equilibrado entre sobre e sub-predição")
        bits.append(lean)
    if result.final_pred is not None and result.final_data is not None:
        bits.append(f"no fim do ensaio: modelo {result.final_pred:.3f} vs "
                    f"artigo {result.final_data:.3f} "
                    f"(&#916; {result.final_pred-result.final_data:+.3f})")
    return "<p class='narr'>" + " · ".join(bits) + ".</p>"
```

No `case_report_html`, dentro do ramo `result.ok` com curva: computar
`pred = np.interp(dx, mx, my)`, `stages = _stage_maes(dx, dy, pred)` e montar
a seção **"3. Resultado e erro"** = métrica grande + `_error_narrative` +
gráfico principal + `_svg_residual` + tabela de estágios:

```python
        stage_rows = "".join(
            _row(f"Estágio {k} ({rng})", _fnum(v),
                 "" if v is None else ("no alvo" if v <= 0.1 else "acima do alvo 0.1"))
            for (k, rng), v in zip((("I", "0-10% ciclos, assentamento"),
                                    ("II", "10-70%, perda principal"),
                                    ("III", "70-100%, cauda")), stages.values()))
```

CSS novo (juntar ao `_CSS`): `.band{fill:var(--accent);opacity:.12}`
`.zl{stroke:var(--fg);stroke-width:.8}` `.rl{stroke:var(--err);stroke-width:2}`
`.narr{font-size:.85rem;max-width:75ch}`.

- [ ] **Step 4: `ast.parse` + rodar** — `python -m pytest tests/test_validation_report_html.py -q` → verde (existentes + 2 novos).
- [ ] **Step 5: Commit** — `git commit -m "feat(validation): report v2 — residuo assinado + narrativa de erro + MAE por estagio"`

---

## Task 2: Seção MSD reproduzível + glossário + passo-a-passo

**Files:**
- Modify: `src/bolt_analysis_studio/validation/report_html.py`
- Test: `tests/test_validation_report_html.py` (append)

**Interfaces:**
- Produces: `_msd_section(rec) -> str` (seção "2. Modelo MSD" completa); `_GLOSSARY: dict` (símbolo → (descrição PT, unidade)); `_svg_chain(elements) -> str`.

- [ ] **Step 1: Testes falhando** — append:

```python
def test_msd_section_lists_real_elements_and_loading():
    from bolt_analysis_studio.validation.case_registry import record
    from bolt_analysis_studio.validation.report_html import _msd_section
    html = _msd_section(record("liu2025_M16_amp0p25"))
    assert "GROUND" in html                          # cadeia real do build_case_model
    assert "k [N/m]" in html                         # tabela de elementos
    assert "Carregamento global" in html
    assert "Para refazer no software" in html
    assert "Glossário" in html and "C_creep" in html


def test_msd_section_degrades_for_other_family():
    from bolt_analysis_studio.validation.case_registry import all_records
    from bolt_analysis_studio.validation.report_html import _msd_section
    rec = next(r for r in all_records() if r.family == "other")
    html = _msd_section(rec)                         # nao levanta
    assert "não parametrizado" in html or "nao parametrizado" in html
```

- [ ] **Step 2: Rodar e ver falhar.**
- [ ] **Step 3: Implementar** (em `report_html.py`):

```python
_GLOSSARY = {
    "F₀": ("pré-carga inicial de aperto do parafuso", "N"),
    "F/F₀": ("razão de pré-carga retida (1 = aperto intacto, 0 = solto)", "–"),
    "δ (delta_amplitude)": ("amplitude do deslocamento transversal imposto (Junker)", "mm"),
    "F_amp": ("amplitude da força cíclica (modo força / axial)", "N"),
    "k": ("rigidez do elemento MSD", "N/m"),
    "c": ("amortecimento do elemento MSD", "N·s/m"),
    "m": ("massa do elemento MSD", "kg"),
    "k_b": ("rigidez axial do parafuso = E·A_s/L_eff", "N/m"),
    "A_s": ("área de tensão da rosca (ISO 898-1)", "mm²"),
    "L_eff": ("comprimento efetivo (grip) do parafuso", "mm"),
    "d₂": ("diâmetro de passo da rosca (ISO 724)", "mm"),
    "r_bearing / A_contact": ("raio efetivo e área real do anel de apoio da cabeça", "mm / mm²"),
    "µ": ("coeficiente de atrito (rosca = apoio neste modelo)", "–"),
    "Rz": ("classe de rugosidade das superfícies (tabela VDI de assentamento)", "µm"),
    "emb_depth": ("profundidade total de assentamento (embedding) da pilha", "m"),
    "N_emb": ("constante de tempo do assentamento (ciclos p/ ~63%)", "ciclos"),
    "C_creep": ("constante de creep do par tribológico (Norton)", "1/(Pa·s)ᵃᵖʳᵒˣ"),
    "k_wear_spec": ("desgaste específico K/H (Archard identificável)", "1/Pa"),
    "tr_loose_gain": ("ganho do afrouxamento rotacional transversal", "–"),
    "W_conf_ref": ("energia de referência da conformação dependente de pressão", "J"),
    "slip_onset_W": ("energia de incubação até o início do slip macroscópico", "J"),
    "c_bend": ("fator de rigidez de flexão do parafuso no k_tr", "–"),
    "loose_arrest_floor": ("piso de auto-travamento do afrouxamento", "F/F₀"),
    "k_ratchet": ("taxa do ratchet cinemático por ciclo de slip", "–"),
    "delta_free": ("folga/take-up antes do carregamento morder", "m"),
    "conform_driver / k_tr_mode / …": ("chaves de modo do engine V2 (formas ativas)", "–"),
}


def _svg_chain(elements, W=560):
    """Cadeia de elementos como no builder: caixas em coluna, conexao serie."""
    BH, GAP, BW = 30, 12, 300
    H = len(elements) * (BH + GAP) + GAP
    x = (W - BW) / 2
    s = [f'<svg viewBox="0 0 {W} {H}" class="plot chain" '
         f'xmlns="http://www.w3.org/2000/svg">']
    for i, el in enumerate(elements):
        y = GAP + i * (BH + GAP)
        typ = getattr(el.type, "name", str(el.type))
        s.append(f'<rect x="{x}" y="{y}" width="{BW}" height="{BH}" rx="6" '
                 f'class="box {"ground" if typ == "GROUND" else ""}"/>')
        s.append(f'<text class="bt" x="{W/2}" y="{y+BH/2+4}" text-anchor="middle">'
                 f'#{el.id} {typ} — {el.name or "–"}</text>')
        if i < len(elements) - 1:
            s.append(f'<line class="cn" x1="{W/2}" y1="{y+BH}" '
                     f'x2="{W/2}" y2="{y+BH+GAP}"/>')
    s.append('</svg>')
    return "".join(s)


def _msd_section(rec) -> str:
    """Secao '2. Modelo MSD' do modelo REAL (build_case_model — o mesmo do
    'Abrir no Model/Run'), reproduzivel so lendo (spec §2.2)."""
    try:
        from .gui_bridge import build_case_model
        model = build_case_model(rec)
    except Exception as exc:
        return (f'<h2>2. Modelo MSD (junta)</h2><p class="verd">Modelo não '
                f'montável para este caso ({exc}) — carregamento não '
                f'parametrizado no runner v1.</p>')
    els = list(model.elements)
    rows = "".join(
        f'<tr><td>{e.id}</td><td>{getattr(e.type, "name", e.type)}</td>'
        f'<td>{e.name or "–"}</td><td class="v">{_fnum(e.msd.k)}</td>'
        f'<td class="v">{_fnum(e.msd.c)}</td><td class="v">{_fnum(e.msd.m)}</td>'
        f'<td>{getattr(e.material, "name", "–")}</td>'
        f'<td class="v">{_fnum(getattr(e, "preload_percent_yield", None))}</td></tr>'
        for e in els)
    gl = model.global_loading
    load_rows = [
        _row("Tipo de carga", getattr(getattr(gl, "type", None), "name",
                                      str(getattr(gl, "type", "–")))),
        _row("Modo de controle", getattr(gl, "control_mode", "–"),
             "displacement = Junker (δ imposto); force = servo-hidráulico"),
        _row("F₀ pré-carga", _fnum(getattr(gl, "F_preload", None), "N")),
        _row("Amplitude δ", _fnum(getattr(gl, "delta_amplitude", None), "mm")),
        _row("Frequência", _fnum(getattr(gl, "frequency", None), "Hz")),
        _row("N ciclos", _fnum(getattr(gl, "n_cycles", None))),
        _row("µ (atrito)", _fnum(getattr(gl, "mu_initial", None) or
                                 getattr(model, "mu_initial", None))),
    ]
    gloss = "".join(
        f'<tr><td class="k">{sym}</td><td>{desc}</td><td class="v">{unit}</td></tr>'
        for sym, (desc, unit) in _GLOSSARY.items())
    steps = ('<ol class="steps">'
             '<li><b>Pelo software:</b> módulo <i>Results → Validation</i>, '
             'selecionar o caso, <i>Abrir no Model/Run</i> — monta exatamente '
             'este modelo.</li>'
             '<li><b>Manualmente:</b> File → Nova Análise (wizard) com o '
             'parafuso/preset acima; no PropertyInspector, aba Loading, '
             'aplicar a tabela de carregamento; aba Contact, aplicar µ; '
             'as constantes V2 da seção 5 entram pela calibração '
             '(overrides de material) e a geometria fina pelo caso.</li></ol>')
    return (f'<h2>2. Modelo MSD (junta) — como preparado no software</h2>'
            f'{_svg_chain(els)}'
            f'<h3>Elementos</h3>'
            f'<div class="ovx"><table class="wide"><thead><tr><th>id</th><th>tipo</th>'
            f'<th>nome</th><th>k [N/m]</th><th>c [N·s/m]</th><th>m [kg]</th>'
            f'<th>material</th><th>preload %yield</th></tr></thead>'
            f'<tbody>{rows}</tbody></table></div>'
            f'<h3>Carregamento global</h3><table>{"".join(load_rows)}</table>'
            f'<h3>Glossário de variáveis</h3>'
            f'<div class="ovx"><table class="wide"><thead><tr><th>símbolo</th>'
            f'<th>descrição</th><th>unidade</th></tr></thead>'
            f'<tbody>{gloss}</tbody></table></div>'
            f'<h3>Para refazer no software</h3>{steps}')
```

CSS extra: `.box{fill:var(--card);stroke:var(--accent);stroke-width:1.2}`
`.box.ground{stroke:var(--mut);stroke-dasharray:4 3}`
`.bt{fill:var(--fg);font-size:10px;font-family:Consolas,monospace}`
`.cn{stroke:var(--accent);stroke-width:1.5}` `.ovx{overflow-x:auto}`
`.steps{font-size:.85rem}` `h3{font-size:.85rem;color:var(--fg);margin:12px 0 4px}`
`th{text-align:left;color:var(--mut);font-weight:600;font-size:.72rem;padding:4px 8px;border-bottom:1px solid var(--bd)}`.

No `case_report_html`: substituir as `msd_rows` atuais (que ficam como
sub-tabela "Geometria do parafuso" DENTRO da nova seção 2, antes dos
elementos) e chamar `_msd_section(rec)`; a geometria (d₂/A_s/L_eff/k_b/anel)
continua aparecendo — movida para dentro da seção 2 como `<h3>Geometria do
parafuso</h3>`.

- [ ] **Step 4: `ast.parse` + rodar** — suíte do report verde.
- [ ] **Step 5: Commit** — `git commit -m "feat(validation): report v2 — secao MSD reproduzivel (cadeia real + elementos + glossario)"`

---

## Task 3: Formatação 1-6 + print + regenerar 128 + artifact

- [ ] **Step 1: Teste falhando** — append:

```python
def test_report_sections_numbered_and_print_css():
    from bolt_analysis_studio.validation.case_registry import all_records
    from bolt_analysis_studio.validation.report_html import case_report_html
    rec = next(r for r in all_records()
               if r.case_class == "full_curve" and r.family == "transverse")
    html = case_report_html(rec, _fake_result(rec.case_id))
    for h in ("1. Condições de contorno", "2. Modelo MSD",
              "3. Resultado e erro", "4. Decomposição por mecanismo",
              "5. Constantes usadas", "6. Caveats"):
        assert h in html, h
    assert "@media print" in html
```

- [ ] **Step 2: Implementar**: renumerar os `<h2>` no `case_report_html`
  (decomposição vira "4. …", constantes "5. …", caveats "6. Caveats e
  veredicto" — a seção 6 passa a ser SEMPRE emitida, com "sem caveats
  registrados" quando vazia); resumo executivo no topo (uma linha sob o h1:
  família · classe · MAE grande · veredito curto); `@media print` no `_CSS`:
  `@media print{body{background:#fff;color:#111;padding:0}.plot,table{break-inside:avoid}.back{display:none}}`.
- [ ] **Step 3: Suíte completa do pacote** — `python -m pytest tests/test_validation_*.py -q` → verde.
- [ ] **Step 4: Regenerar os 128** — `PYTHONPATH=src python -W ignore -m bolt_analysis_studio.validation.report --from-store` (sem re-simular; usa o store atual). Inspecionar bytes UTF-8 + seções de 2 reports.
- [ ] **Step 5: Atualizar o artifact** do liu2025 (mesmo file path da sessão → mesma URL).
- [ ] **Step 6: Commit final** — reports regenerados + nota no STATUS do Plano A ("report v2 2026-07-10: resíduo assinado, MSD reproduzível, glossário — spec 2026-07-10-report-v2-design.md").

---

## Self-Review

**Spec coverage:** §2.1 erro (Task 1), §2.2 MSD reproduzível + glossário + passo-a-passo + degradação `other` (Task 2), §2.3 formatação/print + regeneração + artifact (Task 3). ✔
**Placeholder scan:** limpo. ✔
**Type consistency:** `_stage_maes(cd, rd, pred)` def/uso consistentes; `_svg_residual(dx, dy, mx, my, mae)`; `_msd_section(rec)` usado no `case_report_html` (Task 3 renumera em volta). `e.msd.k/c/m`, `e.material.name`, `e.preload_percent_yield` conferidos contra `element.py`. ✔
**Riscos:** (a) `build_case_model` por report ×128 na regeneração — ~segundos no total, aceitável; import lazy protege o gerador headless; (b) SVG da cadeia com ~12 elementos ok, >20 fica alto — aceito (scroll).
